import 'package:flutter/material.dart';

class ContentEmptyState extends StatelessWidget {
  const ContentEmptyState({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('قائمة المحتوى'));
  }
}
